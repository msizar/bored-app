export interface IChip {
  title: string;
  active: boolean;
}

export interface IActivity {
  activity: string;
  type: string;
  participants: number;
  price: number;
  key: string;
  _id: string;
}